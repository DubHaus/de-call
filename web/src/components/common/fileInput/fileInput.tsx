import Container from '../container';

const FileInput = () => {
    return (
        <Container>
            <input type="file" className="" />
        </Container>
    );
};

export default FileInput;
